name: Crawl & Notify

on:
  workflow_dispatch:
    inputs:
      mode:
        required: true
        default: "refresh"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run refresh
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          VAPID_PUBLIC_KEY: "BC1eSbg-J7HgWPAd_LJUvCH_fggJsdlvHLEeQUzwVzRcLHv27BiGpe1RlU-bUgduNWNRa15JcEayDFJzNRo07fM"
          VAPID_PRIVATE_KEY: "t-H3HMaJBDtxCvyB2RCVBBg-dFmCpxjQX9Yv0FPx8o4"
          VAPID_SUBJECT: "mailto:ccoo2000@naver.com"
        run: |
          python refresh_and_notify.py
